import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputContainer: {
    width: 300,
    gap: 10
  },

  label: {
    fontSize: 24,
    fontWeight: "bold",
    textTransform: "capitalize",
    padding: 8
  },

  input: {
    borderWidth: 1,
    borderColor: "#fff",
    borderRadius: 8,
    padding: 16,
    height: 50,

  },
});